Cache
 |
 +-- CacheStorage
 |      └── value + expirationTime
 |
 +-- ExpirationService
 |      └── removes expired entries
 |             |
 |             └── also removes key from EvictionPolicy
 |
 +-- EvictionPolicy
 |      └── only used when capacity is still full
 |
 +-- WritePolicy
        └── controls Cache + DB writes